# pragma: allowlist secret
# DOPPLER REQUIRED FOR SECRETS MANAGEMENT
# This project uses Doppler for secure environment variable management
# DO NOT create .env files for secrets in production

# To set up Doppler:
# 1. Install Doppler CLI: https://docs.doppler.com/docs/cli
# 2. Login: doppler login
# 3. Setup project: doppler setup

# Required Doppler secrets:
API_SECRET_KEY=required
JWT_SECRET_KEY=required

# Optional configuration (can be set in environment):
DATABASE_URL=optional
APP_ENV=optional
DEBUG=optional
LOG_LEVEL=optional
API_HOST=optional
API_PORT=optional
